apiVersion: v1
kind: Pod
metadata:
  name: citizen-services-pod
spec:
  containers:
    - name: citizen-services
      image: lukasztomalczyk/citizenbudget.services.citizen:1.4
      imagePullPolicy: Always
      env:
        - name: ASPNETCORE_ENVIRONMENT
          value: Production
      volumeMounts:
      - name: config
        mountPath: /app
  volumes:
  - name: config
    configMap:
      name: citizen-service-settings-env-production
  restartPolicy: Never